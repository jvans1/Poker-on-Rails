<h1>Listing handhistories</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>
<script type="text/javascript" charset="utf-8">
  $(function() {
    var highchartsOptions = Highcharts.setOptions(Highcharts.theme);

      new Highcharts.Chart({
        chart: {
          renderTo: "winnings_day"
      },
      title: {
        text: "Winnings Per Day"
      },
      xAxis: {
        type:"date"

      },
      yAxis: {
        title: {
          text: "Big Blinds"
        }
      },

      series: [{
        pointInterval: <%= 7.days * 1000 %>,
        pointStart: <%= 1.year.ago.at_midnight.to_i * 1000 %>,
        data: <%=Handhistory.winnings_over_days%>
      }]
      });
  });
</script>
 

<div id='winnings_day' style = "width:560px; height:300px"></div>
<%# @handhistories.each do |handhistory| %>
  <tr>
    <td><%#= link_to 'Show', handhistory %></td>
    <td><%#= link_to 'Edit', edit_handhistory_path(handhistory) %></td>
    <td><%#= link_to 'Destroy', handhistory, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<%# end %>
</table>

<br />

<%= link_to 'New Handhistory', new_handhistory_path %>
